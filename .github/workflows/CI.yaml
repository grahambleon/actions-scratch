name: Action!

on:
  push:
    branches:
    - master

jobs:
  run:

    runs-on: ubuntu-latest
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

    steps:
    - uses: actions/checkout@v1

    - name: Does this work?
      run: docker run -d quay.io/vgsi/tax-postgres:master